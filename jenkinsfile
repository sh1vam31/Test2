pipeline {
    agent any

    stages {

        stage('check out'){
            steps{
            git url: 'https://github.com/sh1vam31/DevopsClass.git', branch:'main'
        }
        }

        stage('Build') {
            steps {
                sh 'build again'
            }
        }

        stage('test'){
            steps{
                sh 'test again'
            }
        }

        stage('deploy'){
            steps{
                sh 'deploy again'
            }  
        }
    }
}